import 'package:flutter/material.dart';

double width(context,num)=>MediaQuery.of(context).size.width*num;
double height(context,num)=>MediaQuery.of(context).size.height*num;
bool scure=true;
var passwordIcon=Icons.visibility_off_outlined;
 var token;
